<table>
<% @games.each do |game| %>
    <tr>
        <td><%= link_to game.away, team_path(game.away) %> @  <%= link_to game.home, team_path(game.home) %></td><td> <%= game.game_time.in_time_zone("Eastern Time (US & Canada)").strftime "%B %-d, %Y @ %I:%M %p"  %></td>
</tr>
<% end %>
</table>
